'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function TeamsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Teams</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is where your teams will be displayed.</p>
      </CardContent>
    </Card>
  );
}
